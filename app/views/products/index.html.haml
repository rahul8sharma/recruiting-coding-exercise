%h1 Check out our products!

.row.product-list
  - @products.each do |product|
    %article.product
      %h2= product.name
      %p= product.description
      %strong{class: "currency-tag", data: {id: product.id}}
        = product.get_price_by_currency(current_currency)
        = current_currency

      = link_to 'Show more', product

